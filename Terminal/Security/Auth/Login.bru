meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{PATH}}/auth/login
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "email": "fernando@example.com",
    "password": "Micontraseña123!"
  }
}
